@model IEnumerable<ShareCircle.Models.Strosek>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.StevilkaStroska)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.CelotniZnesek)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.DatumPlacila)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Placnik)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Skupina)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.StevilkaStroska)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.CelotniZnesek)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.DatumPlacila)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Placnik.Ime)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Skupina.ImeSkupine)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.ID">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.ID">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.ID">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
